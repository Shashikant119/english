# English Learning Notes

This repository contains beginner-friendly English learning notes with Hindi explanations, practical examples, exercises, and professional sentences.

## Notes

- [Step-by-step grammar roadmap](english-grammar-step-by-step-roadmap.txt)
- [Complete English tense guide](english-grammar-tenses.txt)
- [Word reading and pronunciation guide](english-word-reading-pronunciation-guide.txt)
- [Vocabulary](Vocabulary.txt)
- [Message templates](message-send-notes.txt)
- [General notes](Notes.txt)

## Run on a local server

Open a terminal and enter the project directory:

```bash
cd /var/www/english
```

Start a local server:

```bash
python3 -m http.server 8000
```

Open this address in your browser:

```text
http://localhost:8000
```

Stop the server by pressing `Ctrl+C` in the terminal.

## Push the project to GitHub

Check the changed files:

```bash
git status
```

Add all required files:

```bash
git add .
```

Create the first commit:

```bash
git commit -m "Add English learning notes"
```

Push the `main` branch:

```bash
git push -u origin main
```

For future updates:

```bash
git add .
git commit -m "Update English learning notes"
git push
```

## Repository

GitHub remote:

```text
git@github.com:Shashikant119/english.git
```
